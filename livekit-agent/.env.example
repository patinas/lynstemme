# Keep real values in LiveKit Cloud secrets, never in Git.
LIVEKIT_URL=wss://REPLACE.livekit.cloud
LIVEKIT_API_KEY=replace
LIVEKIT_API_SECRET=replace
GROQ_API_KEY=replace
GOOGLE_API_KEY=replace
# Cloudflare endpoint that reserves one free TTS attempt before Gemini is called.
TTS_RESERVATION_URL=https://REPLACE.example/internal/tts/reserve
TTS_RESERVATION_SECRET=replace
